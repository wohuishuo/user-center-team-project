# 复制为 .env 并填入真实值(.env 已被 .gitignore 忽略,不会提交)
# docker compose 会自动读取同目录的 .env

# MySQL root 密码
DB_ROOT_PASSWORD=change_me_in_prod

# JWT 签名密钥(至少 32 字节)
JWT_SECRET=please-change-this-to-a-long-random-secret-key
